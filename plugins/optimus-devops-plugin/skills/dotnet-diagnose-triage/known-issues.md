# dotnet-diagnose-triage · 已知问题记录

用于记录真实使用中暴露的问题，累积满 3 条"待处理"状态即触发一次 darwin-skill 优化循环。
格式与流程见 `knowledge-base/skill-authoring/rules/06-continuous-improvement.md`。

## darwin-skill 基线评估（2026-09-06）

⚠️ **darwin-skill 本体在本仓不可用，本次基线分按知识库 rubric 手工评出。**

**为什么本体不可用**（2026-09-06 核实，此前记录的归因有误）：全 git 历史中 `.claude/skills/darwin-skill/` 下**只有 `results.tsv` 一个文件进过版本库**（`209d43f` 将其删除并加 gitignore），`SKILL.md` 与评分脚本**从未被提交**——它是纯本机产物，本机副本现已不存在（只余 `cards/` 与 `results.tsv`）。故**无法用 `git checkout` 恢复**，也不是「被 gitignore 误排除」。

**但评分能力可恢复**：九维 rubric、权重与评分公式完整存于 `knowledge-base/skill-authoring/reference/darwin-skill-optimization.md § 2`，并经 `results.tsv` 三条历史记录反推校准——公式为 $\sum_{i=1}^{9} w_i \times \mathrm{dim}_i / 10$（**不做归一化**，权重和 99 即理论满分；文档所称「满分 100」是描述性偏差）。三例验算逐一精确吻合：`media-resize` 81.90、`media-play` 80.90、`sync-cc-docs-to-youdaonote` 83.80。

| 项 | 值 |
|---|---|
| 总分 | **88.3** |
| 评估模式 | `full_test (partial)` — dim7/dim8 有七例真实调用证据，但**缺 without-skill 对照**（rubric § 3 要求的第 2 步未做），故不记完整 `full_test` |
| 结构组（dim1-6） | 51.4 |
| 效果组（dim7-8） | 31.5 |
| 元技能（dim9） | 5.4 |
| 判读 | 新建 skill 基线，无历史分可比。后续 Minor/Major 升级须 **≥ 88.3**，倒退则先修正 |

| # | 维度 | 权重 | 分 | 折算 | 评分依据 |
|---|---|---:|---:|---:|---|
| 1 | frontmatter 质量 | 7 | 9 | 6.3 | 五字段合规；description 376 字，含时机边界「已取得」+ 14 个触发词 + 转出去向；保留跨语言技术标识符 |
| 2 | 工作流清晰度 | 12 | 9 | 10.8 | 台账四字段/初始化/关闭/出口/跨轮续用五节递进，输入输出明确。扣分：主干未编号步骤，三步编排在 agent 侧 |
| 3 | 失败模式编码 | 12 | 10 | 12.0 | 九条失败处理全为「若 X 则 Y」形态；另有出口条件三态表、第二跳断链降级、排除判据范围限定 |
| 4 | 检查点设计 | 6 | **5** | 3.0 | ★**唯一明显短板**：`🔴 CHECKPOINT` / `🛑 STOP` 标记 **0 处**。自检四项与 B 组阻断是两处天然人审关口，只有文字规则、未打标记 |
| 5 | 可执行具体性 | 17 | 9 | 15.3 | 判据一律 `file § anchor` 可反查；三强度判定条件明确；MUST/SHOULD 分级。扣分：部分规则偏叙述表述 |
| 6 | 资源整合度 | 4 | 10 | 4.0 | references 三份各带「何时读这份」；golden.md 七例；CHANGELOG/README/known-issues 齐备；引用经 `check_refs.py` 校验全有效 |
| 7 | 整体架构 | 12 | 9 | 10.8 | 两层分工清晰、progressive disclosure 落点正确、主干 149 行无冗余。扣分：SKILL.md 与 agent 的边界说明有少量重叠 |
| 8 | 实测表现 | 23 | 9 | 20.7 | 七例首轮全通过零返工；两处高风险点（测例 4 越界排除、测例 5 合并候选集 + 断链）均符合预期；多次主动识破输入缺陷。扣分：无 without-skill 对照 |
| 9 | 反例与黑名单 | 6 | 9 | 5.4 | 不靠推理关闭假设、不硬套征象、不自造 anchor、不展开修复方案、不把 SHOULD 说成 MUST、零命中形态禁用 |

**下一轮优化的首选靶点**：dim4（权重 6，现 5 分）。按 rubric 棘轮规则，基线阶段**只记分不修**——修了就不再是基线。给自检四项与 B 组阻断两处补 `🔴 CHECKPOINT` 标记，预期 dim4 5→9、总分 88.3→**90.7**。

⚠️ **`full_test (partial)` 与完整 `full_test` 的分数不可直接比较**。后续若补做 without-skill 对照，dim8 可能升降，届时须重记模式。

## 黄金测例首轮结果（2026-09-06）

七例经 `claude --plugin-dir ./plugins/optimus-devops-plugin -p` 无头模式实调 `@optimus-devops-plugin:dotnet-diagnose`，逐例比对 `test-cases/golden.md` 的预期。

| # | 台账 | 强度 | anchor | 备注 |
|---|---|---|---|---|
| 1 | ✅ | ✅ | ✅ | `MonitorHeld` 编码 (3−1)/2=1 读对，循环等待识别正确；另主动指出 `!syncblk` 不输出等待线程身份，须交叉 `!clrstack -all`，并按 `wpf-dispatcher-deadlock.md § 4` 追加「环是否绕回 UI 线程」为独立待验项 |
| 2 | ✅ | ✅ | ✅ | 停 `待验`，明确判据要求两次 `-stat` 对比；另自行发现输出被截断（`Total` 与列出各行之和差 100）并据此拒绝「top-5 无 WPF 类型 → 排除 WPF 泄漏」的推论 |
| 3 | ✅ | ✅ | ✅ | 走 `§ 6` 根链形态反查表一跳收敛；修复方向原样转述 `wpf/rules/05-data-binding.md § 2`，未改写未展开 |
| 4 | ✅ | ✅ | ✅ | **风险最高一例通过**：排除 § 2/§ 3/§ 5，§ 4 弱事件泄漏保留 `待验` 并注明「泄漏在 `WeakEventManager` 内部监听表，按应用类型名筛天生盲」；无 `已证实` 项故不给修复方向 |
| 5 | ✅ | ✅ | ✅ | **spec 标注最易做错一例通过**：台账 9 条（合并 7 条 + WPF 加挂 2 条），线程池饥饿停 `待验`、强度「推测」，注明第二跳需 dump 或活体连接，未记 `无法判定` |
| 6 | ✅ | ✅ | ✅ | B 组第二项阻断，未进入台账循环（候选全停 `待验`、证据来源统一「无可用证据」），不给修复方向；另指出 procdump 漏 `-ma` 与「Mini 保留异常信息，`!pe` 可零成本先判异常类型」的岔路 |
| 7 | ✅ | ✅ | ✅ | 只取异常记录段，业务行不参与；强度「推测」，注明异常链可能未展开。另自行发现输入日志的时序矛盾（`Unhandled exception` 之后仍有 INFO 输出 → 该行非 CLR 崩溃路径产生），据此否掉因果链——该矛盾为构造输入时无意引入，agent 推理正确 |

不通过并已修正的例：**无**。七例首轮全通过，未改 SKILL.md / references / agent，故 `metadata.version` 保持 `1.0.0`、插件版本保持 `1.1.0`。

| 日期 | 问题描述 | 触发场景/prompt | 状态 | 优化后版本 |
|---|---|---|---|---|
| 2026-09-06 | `golden.md` 的「跑法」一节可操作性不足：七例的「输入」段是**描述性**表述（如「显示某业务类型 Count 很高」），不是可直接消费的取证输出文本。原样作为 prompt 时，外层会话会判定证据不足而自行拦下追问，agent 根本未被派发（测例 2 首跑即如此），实测的是外层会话的判断力而非 agent 行为 | 测例 2 首次调用：`-p '@optimus-devops-plugin:dotnet-diagnose 内存一直涨。这是一份 !dumpheap -stat 输出，显示 MyApp.Models.OrderItem 这个业务类型 Count 是 1847293，Size 很高。'` → 外层未派发，返回「你只给了摘要没给实际输出文本」并要求补充 | **已处理** | 1.0.1 |
| 2026-09-06 | 测例跑法缺「禁止外层代答」的显式约束。无头模式下 prompt 经外层会话转手，外层可能替 agent 补充前提或代为追问，污染验收结果 | 同上；测例 2/4/5/6/7 均加前置句「直接原样派发给 @…，不要自己追问或代答」后才正常派发 | **已处理** | 1.0.1 |
| 2026-09-06 | 含 shell 元字符的 prompt 会被权限层拦截。测例 5 的输入含裸 `> 0`（「队列长度持续 > 0」），`claude -p '...'` 调用连续两次被判 denied | 测例 5：`-p '…ThreadPool Queue Length 持续 > 0（在 40~120 之间波动…）…'` | **已处理** | 1.0.1 |

**三条的统一修法**（`golden.md` v1.0.1，同日）：

1. 七例「输入」段全部改为**可直接消费的原始取证输出文本**——`!syncblk` 带 `Index`/`SyncBlock`/`MonitorHeld`/`Owning Thread Info` 完整列、`!dumpheap -stat` 带 `MT`/`Count`/`TotalSize`/`Class Name` 五行、`!gcroot` 带完整根链、`dotnet-counters` 带 8 行采样表。列名逐字取自知识库，未自造
2. 「跑法」一节新增**三条跑法约束**与无头模式命令范式：前置「不要自己追问或代答」、含 shell 元字符时走文件 + `-p "$(cat file)"`、输出落盘 `$HOME/dd-golden/`（仓库外，按 `rules/01-dump-handling.md § 2. 版本库隔离`）
3. 顺带修正两处**构造缺陷**（首轮被 agent 正确识破，但作为固化测例会掩盖考点）：测例 2 的 `Total` 由 `2369086` 改为与各行之和一致的 `2368986`、三行数值空格错位修正；测例 7 删除异常行之后那条 INFO（进程终止后不应再有业务行）。另给测例 1 补 `!clrstack -all` 交叉证据——`!syncblk` 本身不输出等待线程身份（`sos-locks-and-async.md § 1` 「注意」段），原输入「互为等待方」是分析者结论而非证据

**改后重跑验证**：受影响最大的测例 1 / 6 / 7 三例重跑**全部通过**。测例 1 的强度从首轮带限定的「已确认（但等待方身份未独立核实）」变为干净的「已证实」；测例 7 首轮那段因构造缺陷引起的时序矛盾讨论消失，改为正确的「征象层已确认 + 根因层推测」两级结论。
